import React from 'react'
 export default function AdddataButton({props}) {
  return (
    <div>
      
      <button   onClick={props.data} >
            Add Data
          </button>

    </div>
  )
}
